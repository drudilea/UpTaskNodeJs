extends layout

include mixins/create-project

block content
    .contenido-principal
        h1 #{pageTitle}

        if errors
            each error in errors
                .alerta.error= error.msg
        +create-project(project)
